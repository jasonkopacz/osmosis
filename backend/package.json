{
  "name": "backend",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed": "npx wrangler d1 execute osmosis --file=seed_all.sql --remote",
    "enrich": "npx wrangler d1 execute osmosis --file=enrich_all.sql --remote"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@rolldown/binding-darwin-x64": "^1.0.0-rc.15",
    "hono": "^4.12.12",
    "stripe": "^22.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260414.1",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.9.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4",
    "wrangler": "^4.82.2"
  }
}
